import React from 'react';
import { LoaderWrapper, LoaderIcon } from './Loader.styles';

const Loader = () => {
    return (
        <LoaderWrapper>
                <LoaderIcon />
        </LoaderWrapper>
    )
}

export default Loader;